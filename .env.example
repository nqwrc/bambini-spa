# bambini-spa currently reads no environment variables — a static Vite/
# Tailwind site with no process.env/import.meta.env usage. Kept as a template
# per the project root interface (nqwrc.dots setup/01, "Standard Internal
# Structure for Every Project Folder") for whenever one is needed.
